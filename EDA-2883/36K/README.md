# Verilator/iVerilog Simulation for TDP_RAM36K:

To run simulation on Verilator, run the following command:
```
make verilator
```

To run simulation on iVerilog, run the following command:
```
make iverilog
```

To clean directory, run the following command:
```
make clean
```